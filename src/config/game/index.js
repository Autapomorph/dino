import SCENES from './scenes';
import PREFABS from './prefabs';
import EVENTS from './events';

const CONFIG = {
  GAME: {
    WIDTH: {
      PORTRAIT: 600,
      LANDSCAPE: 1200,
    },
    HEIGHT: 350,
  },
  SCENES,
  PREFABS,
  EVENTS,
};

export default CONFIG;
